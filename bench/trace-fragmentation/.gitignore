# Generated by the harness; none of it belongs in git.
# Note the trailing glob: run.sh -o takes an arbitrary directory name, and an
# entry of plain `results/` silently fails to match `results-20000/`.
corpus/
results*/
*.log
smoke/
__pycache__/
